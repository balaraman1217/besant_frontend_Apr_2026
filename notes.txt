HTML - Hyper text markup language 

Everything is based on tags, attributes, values 

tags 



<>  --> open tag
 

</> --> closing tag

self closing 

<html>
     <head>
     
     </head>
     <body>

     <header>
     </header>

     <footer>
     </footer>
     </body>
</html>


tags

Hyperlink - it is digital reference to the data where user can follow by clicking or taping the button or link - <>


Text Formmating

Group Together HTML 

Why we are grouping - Styling purposes 

Block level contianer - div - to group the elements for styling purposes

Inline Container - span - to group the elements for styling purposes

list 

unordered list

order list 

button 

forms

Git and Github ---> Version Control Tool

Git --> For local

GitHub ---> For Internet (Comman data center to store our project or source code)

CSS ---> Cascading styling sheet 

Inline Internal External 


color - color name, rgb values, hexa decimal, hsl values 

borders

Shadows

Margin - space arond the element 

float --- allow other element to flow around it 

Overflow ---> property that sets the desried behavior when content does not fit in the parent element box

overflow:visible
overflow:hidden
overflow:clip
overflow:scroll
overflow:auto



Display property 

display = property specfies if/how an melement is didplayed 

Block-level - start on a new line, take up to full width available(h1, div, p, form, header, footer)

Inline - - do not start on a new line, width is limited to what is needed (span, a, img)

height and width 

Postions -

relative - positioned relative to where it normally is 
fixed - positioned relative to the viewport
absolute - psotioned reletive to nearest ancestor
sticky - psotioned based on scroll postion 
static - default position for an element 


Grid System 

Flexbox 

Transform - css property lets us to rotate, scale, skew, or translate an element

Animations 

Javascript -----> It is programming + scrpting language




Varaiables - container that stores the value.

behaves as if it were the value it contains

let var const 



declartion 

let a;  

initialization or assignment 

let a = 10;


Arithemmtic Operators - operands(values, varaibles, etc) and Operators(+, -, /, %, *)

11 = x * 5;


// How to accept the user input

1. Easy way - window prompt

2. Professional way = HTML testbox


Type conversion - change the datatype of a value to another(strings, numbers, booleans) 


Constant - variable that cant be changed 

Conditional Statement 

if Statement, if else Statement, nested statement 

checked property


Ternary operator - shortcut to if() and else() statements
helps to assign a varibale baesd on a condition


condition ? codIfTrue : codeIfFalse;


switch - can be efficeient replacement to many eles if statements 


string methods - allow you to manipulate ad work with text (strings)

string slice - creating a substring from a portion of another string 

string.slice(start, end)


logiacl operators  used to combine  or manipulate boolean values (true or false)

&& - AND
|| - OR
! - !

Strict equality Operators


= assignment operator 
== comparison operator (compare if values are equal)
=== strict equaltiy operator (coampore if values and datatype are equal)
!= inequality operator - this will return true if two values are not equal 
!== strict inequality 


Control Statement - 

for loop 


for(were we start; where we end; increment or decrement) {



}

task 1

*
1
*
1
*


Task 2 

###
***
@@@



Functions - a section of reusable code, declare code once, use it whenever we want, call the function to execute the code.

Input :- GANesh KumAR
output:- Ganesh Kumar



create a function which return the square root of the value

input : 2
output: 4


write a function for find out area of cirlce 




Variable scope - where a varaible is recogosined and accessible (local vs global)



Array - variable like structure that can hold more that 1 value

Spread operator :-    ...

allow an iteration such as a array or string to expanded  into seprate elements 

unpacks the elements

rest parameters - allow a function work with a vraiable number of argumenets by bundling them into an array 

spread - exapad an array into speate element
rest bundles seperate elements into an array


sperate name and marks 

const students = {
     name: "prakesh",
     age: "24",
     mark: 90,
     city: "chennai"
}




add new values in the array 
[2,3,4] 

[2,3,4,6,7,8,9,9,8,7]


Callbacks - function that is passed as an argumenets to another function;

used to handle asynchrous operation 

Reading the file 
Network requests
INteracting with databases 

"Hey", when you are done, call the next 

// 1. Define the callback function (the task to perform after calculation)
// 2. Define the outer function (calculates the grade)
 // Calculate percentage using LaTeX: $ percentage = (\frac{score}{totalScore}) \times 100 $
   // Trigger the callback function, passing the student details
   // 3. Execute the function and pass the callback as an argument
   // Output: Hello John Doe! Your final grade for the course is A.

   calculateGrade(45, 50, "John Doe", notifyStudent);
// Output: Hello John Doe! Your final grade for the course is A.


forEach() - method used to iterate over the element of an array and apply a sepicfed function(callback) to each element

array.forEach(callback)
// element, index, array are provided


 Price Tag Formatter
 
 Task: Use the optional index parameter to modify strings.Array: [10.99, 5.50, 20.00]
 
 Goal: Log a message like "Item 1: $10.99" for each element.

 URL Slug Generator (String Manipulation)

 Task: Convert a list of blog titles into URL-friendly "slugs".Array: ['Learn JavaScript Fast', 'How to Use Loops', 'Array Methods Guide']
 
 Goal: Create a new empty array. Use forEach to convert each title to lowercase and replace spaces with dashes (e.g., 'learn-javascript-fast'). Then push it to the new array.

native

Map()- accpets the callback and appilies that function to each element of an array, then return a new array.


filter() - creates a new array by filtering out an elements 

.reduce()- reduce the elements of an array to single value


Function declartion - define a reusable block of code that perform a specifc task 


function expression - a way to define function as values or varibales 

// callbacks in asynchrous operations 
// HOC - higher order funtions
// closures
// Event Listners 

Arrow Functions - a concise way to write function expression good for simple functions that we are use only once 

(parameter) => code 


OOPS -> Object oriented programming structure - collections of classes, properties, methods and objects 


Object - a collection of related properties and/or mehtods can represent real world objects(people, products, places)

object = {key: value, function()}



class {
properties, methods, objects


}

This - refrence to the object where THIS is used (the object depends on the imdeiate context)

person.name = this.name;

Constructor - special method for defining the properties and methods of objects 

classes - (ES6 feature)- provides a more structure and cleaner way to work with objects comapred to traditional construtor functions.

class - static, super 

static keyword - keyword that defines properties or methods that belong only to a class itself rather than the objects created from that class (class owns anything static, not the obeject)